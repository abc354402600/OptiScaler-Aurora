# Compatibility branch handoff — 2026-09-15

The user abandoned the RC3 one-click installer branch and authorized compatibility work, local commits, and ordinary pushes to `Compatibility-fixes`. Their latest 2026-09-17 instruction authorizes synchronization to `aurora` after the ongoing code work, review and validation are complete, with real-game testing deferred. No Release or replacement ZIP deliverable.

## Baseline and evidence

- `aurora` / initial `Compatibility-fixes`: `e1673a16673070401612db04cc0593ca4dc3a6f6`.
- Old installer branch remains `aurora-rc3-installer-20260913`; old Witcher experiment remains `experiment/witcher-mfg-frame-sync-20260915`.
- User confirmed Witcher panel and 6X worked after installer fixes, then reproduced crashes loading with saved Dynamic MFG and toggling Dynamic/manual 6X or FG. Slight rapid flicker also remains. This is not a claim that the compatibility fixes have passed in-game testing.
- Existing crash evidence includes GPU DEVICE_HUNG / frame mismatch / Reflex failures, and separate CPU crashes at `witcher3.exe+0x1f1f4ea`. One fix must not be assumed to explain all signatures.
- User logs and dumps remain in the Codex task workspace `work/`, not in this public repository. Never commit private dumps or full user logs.
- See [compatibility audit](COMPATIBILITY_AUDIT_20260915.md) for adopted changes, sources, limitations, and validation.

## Continuation rules

Latest user constraints: no ZZZ installation/feedback is available, and Witcher cannot currently be retested. Keep these as pending validation; continue independent code work without repeatedly requesting game tests. New features are deferred. The latest authorization removes game tests as a merge prerequisite, but does not mean unfinished lifecycle work should be merged or that regression-free game behavior has been established.

Official upstream is `https://github.com/optiscaler/OptiScaler.git`; the local remote named `upstream` is instead the grim-susemi fork. Official audit cutoff is now `a4890db5b3c7c6918f4b35d0fd3318b42e2ccc66`. It adds only the Sword and Fairy 7 quirk after `4af2417b`; adopted with provenance. Nine commits before that, after `731f3b79`, were already reviewed. Do not repeat those audits.

Current package workflow builds this branch and runs production-header frame/handle guard tests. Keep the original installer layout inherited from `aurora`; do not restore RC3 tools.

The 2026-09-16 continuation adds optional input discovery outside the state lock and registry-first provider identity routing, with 8 input-lock and 17 provider checks. Read [lock/lifetime follow-up](COMPATIBILITY_LOCKS_20260916.md) for exact scope and remaining lifecycle risks. Retired provider token storage now intentionally lasts until DLL unload, superseding the earlier audit's entry-destruction description.

The next continuation implements active private Streamline binding after device selection, separates runtime initialization from FG readiness (including Witcher's second-device path), and guards failed Reflex frame-token acquisition. See [Streamline binding audit](STREAMLINE_BINDING_20260916.md), including 23 lifecycle checks and the full-build result. This supersedes the earlier deferred-binding item; provider-global concurrency and actual game verification remain outstanding.

The creation-failure follow-up adds 16 CPU checks and repairs null/stale handle access after failed FG creation in D3D12/Vulkan. See [FG creation audit](FG_CREATION_20260916.md), which also explains why provider construction needs an explicit busy/routing design and why device-specific shutdown cannot simply become a global lock/stop.

The same audit's shutdown follow-up removes duplicate provider Shutdown calls after Shutdown1, guards missing native shutdown exports, and adds 14 executable routing checks compiled from the actual source bodies. It retains existing local cleanup; full multi-device cleanup, native result propagation and global concurrent lifetime ordering remain open.

The user subsequently provided ZZZ 11008 component-error screenshots and reports of disappearing files, and NTE menu stutter with an NPI workaround. GPU is tentatively RTX 40; exact build, missing filenames, protection history, and crash logs remain unknown. See [NTE/ZZZ follow-up](NTE_ZZZ_COMPATIBILITY.md). Do not confuse component errors, GPU exceptions, and absent FG input. No driver/game settings were changed by this work.

Git network requests on this host have worked with per-command `http.proxy=http://127.0.0.1:7897`; do not alter global proxy or disable TLS verification. Work on `origin Compatibility-fixes`; synchronize `aurora` only after completing the authorized code-validation gate above.

The 2026-09-17 continuation fixes private provider construction/publication and explicit Pending routing, plus no-lazy-load shutdown. Read [provider publication audit](PROVIDER_PUBLICATION_20260917.md). Native API/device initialization and concurrent Shutdown/Evaluate after publication remain open; do not call the complete lifecycle audit finished.

The same continuation also initializes DLL-provider depth pointers, guards missing per-operation exports (69 extracted checks), and fixes Combo partial-release ownership/retry (44 checks). See the same publication audit for limitations; this does not establish native API/device shutdown safety or game compatibility.

Latest validated code checkpoint: e737b087 (Windows build 35129887577 and format 35129887470 succeeded; 171 new publication/export/Combo checks passed). The publication audit records remaining native shutdown/device ownership findings, including the separate DLSSFeatureDx12 nested native-shutdown route; continue there rather than repeating these completed fixes.

The next checkpoint `3df84993` fixes native D3D12/Vulkan device-specific Init/Shutdown and removes the nested DLSSFeatureDx12 native shutdown. Windows build `35188944901` and format `35188944895` succeeded; 37 device and 43 extracted shutdown checks passed. See [native lifecycle audit](NATIVE_DEVICE_LIFECYCLE_20260917.md), including the operation-admission follow-up, test counts and exact remaining boundaries. Do not restart this ownership work or rerun unrelated installer suites. The latest user explicitly permits aurora synchronization after code/review/build completion without waiting for game tests; remaining lifecycle boundaries must still be resolved or explicitly scoped before that gate is met.

Checkpoint `914c2518` adds native operation admission, cached-call-pointer guards and correct parameter-destroy failure reporting. Windows build `35189573586` and format `35189573598` succeeded (55 device, 43 shutdown, 236 operation checks). The subsequent native-module publication fix uses a private immutable export table; read [its audit](NATIVE_MODULE_PUBLICATION_20260917.md) rather than reimplementing it. Shared Init path/state metadata and replacement-provider shutdown/handle generations remain unfinished; real-game validation is deferred by the user's request, and aurora synchronization has not yet been performed.

Native module publication is committed in `33ef3e0a` with incremental formatting follow-up `946462b0`. The next scope fix makes delegated-Init skip flags thread-local in D3D11/D3D12/Vulkan and removes unused dangling State::NVNGX_FeatureInfo writes; 24 extracted scope checks pass. Both are detailed in the same module-publication audit. Do not equate these bounded fixes with full State/Config synchronization.

Latest validated code is now **`514a224a`**: Windows build `35218003287`, format `35218003276`, package `OptiScaler_Aurora_v1.0_20260917_compat_514a224a.7z` all succeeded. The module-publication audit records exact artifact identity, focused checks and two negative-control mutations. Next concrete work is owned immutable Init path/metadata snapshots (include the DlssNr_Proxy GetFeatureCommonInfo caller), then replacement-provider device/handle-generation shutdown and result propagation. No aurora synchronization yet; the user authorizes it after code/review/build completion, without waiting for deferred game tests. Avoid repeatedly auditing or testing already completed batches unless the new edits affect them.

The owned-path and metadata follow-up is now complete and pushed: **`3d2963ba`**, **`ef2276cb`**, formatting **`0cbdf8e4`**. Windows builds `35275907485` and `35276701186`, respective format checks and artifacts succeeded. Read [NGX snapshot audit](NGX_INIT_SNAPSHOTS_20260917.md) for 32 path/39 metadata checks, artifact identities and the explicit limitation that global snapshots are not yet per-device transactional configuration. Do not repeat that implementation. The next checkpoint repairs replacement-provider per-handle callback reentry; read [provider reentry audit](PROVIDER_REENTRY_20260923.md). Continue with provider-global device/generation admission and shutdown-result propagation after its build closes. No aurora merge or Release has occurred.

Latest validated/pushed code is **`64f5ef46`**: Windows build/package `35786223489`, format `35786223620` succeeded; 32 registry and 18 extracted Release-route checks plus affected creation/Combo checks pass. The reentry audit records artifact identity, the negative-control failure, and concrete differences between FFX/DLL/Combo shutdown that the next stage must respect. Working tree was clean after the code checkpoint. Next: replacement-provider API/device admission, old-handle generations and partial shutdown outcome propagation. Do not reimplement snapshots or per-token callback admission. `aurora` remains `e1673a16`; ordinary Compatibility-fixes pushes remain authorized, game testing deferred, no Release requested.


The 2026-09-24 continuation adds replacement-provider transition/operation admission (`6f5597ac`), per-API/device attempted-Init cleanup footprints (`19e934e8`), Combo child-level partial-close retry (`d1111d53`) and immutable handle API identity (`5ac7f631`). All are pushed to Compatibility-fixes. Read [provider transition audit](PROVIDER_TRANSITIONS_20260924.md) for 85 admission, 104 complete Init, 132 Combo shutdown and 42 API-route checks, affected regressions and negative controls. Windows build/package `35946609088` and format `35946609043` passed on `19e934e8`; latest-code build/package `35947175730`, format `35947175655` and job `107467622284` all succeeded on `5ac7f631`. The transition audit records artifact id/digest and three negative controls. Do not call the full lifecycle work finished: device/generation identity, outstanding FFX/Combo feature ownership after parent Shutdown, and native Init transaction ordering remain. No aurora merge or Release; continue from these commits rather than reimplementing them.

The 2026-09-25 incremental upstream/fork pass is recorded in [upstream audit](UPSTREAM_AUDIT_20260925.md). New official **inventory cutoff** is `20d9d147a4334a49a99d67d235ad7915e0d6d846` (51 commits after a4890db5), with explicit deferred clusters, not blanket acceptance. Code checkpoint `644bd42e` ports bounded resource guards, three game quirks and build metadata improvements; its resize-skip candidate was rejected because identical dimensions do not prove a valid no-op after fullscreen transitions. Follow-up `bda53045` serializes Nukem settings refresh, restores rejected UI toggles and adds 30 complete-route checks. Both are pushed and validated: upstream-port Windows run `36149754727` / format `36149754411`, final-code Windows run `36150242520` / format `36150242474` all succeeded. Final Windows job `108121375387` confirms the 110 resource and 30 settings checks; exact artifact identity is recorded in both audits. Continue outstanding device/generation/FFX cleanup work afterward; do not recreate these completed changes or call this an aurora merge gate. No game tests, Release or replacement ZIP.

The 2026-09-26 continuation adds API/device-selective live-feature drain before native/provider shutdown, FFX destruction error propagation and device-reference ordering (`1c57db0e`), then suspension of all selected Evaluate routes during partial drain (`5ca3e6ec`). Both are pushed. Read [feature drain audit](FEATURE_DRAIN_20260926.md) for 46 drain + 22 FFX checks, expanded 56 exported shutdown checks, negative controls and conservative unknown-device Vulkan/loader-lock behavior. Both Windows builds passed: `36221428934` on 1c57db0e and final `36221577866` / job `108347716832` on 5ca3e6ec. Format runs `36221428908` and `36221577867` passed. The feature drain audit records final artifact identity and all four negative controls. Next open boundaries: repeated Init without completed teardown, global native Init transaction ordering, private orphan ownership after partial Combo Create, and concurrent ordinary Evaluate shared state. Published-handle shutdown drain is implemented; do not reimplement it as mere token invalidation. User's latest instruction authorizes direct aurora synchronization when mature, without another confirmation or dedicated synchronization notification; no game tests or Release.
